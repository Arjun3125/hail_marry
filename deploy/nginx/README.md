# Nginx Assets

Canonical nginx configs now live here.

- `app.conf`: production-style reverse proxy
- `demo.conf`: demo reverse proxy

Legacy compatibility copies remain at:

- `/nginx.conf`
- `/nginx.demo.conf`

Prefer `deploy/nginx/*.conf` for new deployment work.
