# Comprehensive VidyaOS Test Suite Implementation Plan

Based on the research paper index and the user's requirements, we are proposing a comprehensive test suite covering critical systemic dimensions: Edge Cases, User Flow, Rate Limiting (Backend Scaling), and LLM Evaluation (RAG Accuracy).

## User Review Required

> [!IMPORTANT]
> This plan outlines an extensive expansion of the test suite in the backend directory. 
> Please review the specific test modules below. Are there any particular RAG prompt datasets or specific overload simulation loads (e.g., number of concurrent queries) you'd like to use for testing?

## Proposed Changes

We will introduce comprehensive test files in `backend/tests/` to target the theoretical bottlenecks described in the associated research papers (Overload Control, RAG QA Benchmarking, and Interaction/Flow constraints).

---

### Phase 1: Rate Limiting & Overload Control (From `Backend_Scale`)
Research highlighted the critical nature of overload control and adaptive auto-scaling. The existing tests (`test_rate_limit.py`) will be augmented to simulate high-concurrency overload.

#### [NEW] [test_overload_control.py](file:///c:/Users/naren/Work/Projects/proxy_notebooklm/backend/tests/integration/test_overload_control.py)
This module will test:
- Endpoint behavior under sudden traffic spikes (simulating multiple simultaneous requests).
- Database connection pool exhaustion and graceful failure handling.
- Enforcement of strict rate-limit boundaries across different user tiers (student, teacher, admin).

#### [MODIFY] [test_rate_limit.py](file:///c:/Users/naren/Work/Projects/proxy_notebooklm/backend/tests/test_rate_limit.py)
- Expand existing tests to validate sliding-window algorithms and ensure 429 status codes return the appropriate `Retry-After` headers.

---

### Phase 2: User Flow & State Transitions (From `Psychology_UX` & `Frontend_API`)
Research emphasizes heuristic inspections and end-to-end communicability between the user and the API. 

#### [NEW] [test_end_to_end_flows.py](file:///c:/Users/naren/Work/Projects/proxy_notebooklm/backend/tests/integration/test_end_to_end_flows.py)
This module verifies primary state-machine transitions:
- Student Upload -> AI Processing -> Teacher Dashboard Visibility.
- Ensure that failure at any stage of the user flow leaves the database in a consistent, recoverable state.
- Validate role-based constraints throughout the flow.

---

### Phase 3: LLM Architecture, RAG Evaluation & Edge Cases (From `LLM_Architecture`)
Several papers emphasized benchmarking multi-hop queries, hallucination prevention (THELMA), and learning from negative examples.

#### [NEW] [test_rag_benchmarking.py](file:///c:/Users/naren/Work/Projects/proxy_notebooklm/backend/tests/evaluation/test_rag_benchmarking.py)
- Test edge cases where documents have contradicting information, verifying the LLM component's handling of ambiguity.
- Validate multi-hop query structures for AI Grading.
- Test handling of malformed and extremely long text inputs natively generated by edge-case file uploads.

#### [NEW] [test_llm_negative_handling.py](file:///c:/Users/naren/Work/Projects/proxy_notebooklm/backend/tests/evaluation/test_llm_negative_handling.py)
- Insert "failure/negative" prompts and determine whether the application's AI gateway gracefully catches unsafe or highly inaccurate generations.

---

## Verification Plan

### Automated Tests
- Run `pytest backend/tests/integration` to ensure User Flow and Overload behaviors.
- Run `pytest backend/tests/evaluation` to validate RAG handling and LLM boundaries. 

### Manual Verification
- Manually review test outputs for RAG generation logic to ensure the accuracy meets quality bounds outlined in the research.
